FROM holochain/holochain-rust:circle.02.warm.tests

# @todo make this useful
# docker volumes and circle checkout blow target away so this is useless
# https://github.com/holochain/holochain-rust/issues/808
# RUN nix-shell --run hc-install-cmd

# @todo make this useful
# neon clean blows away rust compilation so this doesn't help
# https://github.com/holochain/holochain-rust/issues/809
# RUN nix-shell --run hc-install-node-container
