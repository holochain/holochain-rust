[package]
name = "hc_core"
version = "0.1.0"
authors = ["Nicolas Luck <nicolas@lucksus.org>"]

[dependencies]
serde = "1.0"
serde_derive = "1.0"
serde_json = "1.0"

[features]
# Treat warnings as a build error.
strict = []
