/// <reference types="node" />
import * as fs from 'fs';
import quicklyCopyFile = require('quickly-copy-file');
export declare let stat: (path: string) => Promise<fs.Stats>;
export declare function readFile(path: string, options: {
    encoding: string;
    flag?: string;
}): Promise<string>;
export declare function readFile(path: string): Promise<Buffer>;
export declare type WriteOptions = {
    encoding?: string;
    mode?: number;
    flag?: string;
};
export declare let writeFile: (path: string, contents: string, options: WriteOptions) => Promise<void>;
export declare let copy: typeof quicklyCopyFile;
export declare let remove: (path: string) => Promise<void>;
export declare let mkdirs: (path: string) => Promise<void>;
