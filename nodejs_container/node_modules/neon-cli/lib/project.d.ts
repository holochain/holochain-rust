import Crate from './crate';
import * as rust from './rust';
export declare type ProjectOptions = {
    crate?: string;
};
/** A Neon project and its directory tree. */
export default class Project {
    readonly root: string;
    readonly crate: Crate;
    constructor(root: string, options?: ProjectOptions);
    build(toolchain: rust.Toolchain | null, release: boolean): Promise<void>;
    clean(): Promise<void>;
}
